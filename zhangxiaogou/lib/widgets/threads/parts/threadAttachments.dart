import 'package:flutter/material.dart';

///
/// ThreadAttachments
/// 一个专门用来抽取主题中附件数据，并为用户呈现的组件
/// 该组件不支持图片附件渲染，用于渲染图片的，是ThreadGalleries
/// 设计目的
/// 用于显示主题中包含的附件
/// 支持用户进行下载。
/// todo: 考证附件下载权限等
/// 
class ThreadAttachments extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}